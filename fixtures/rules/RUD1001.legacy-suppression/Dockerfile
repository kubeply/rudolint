# hadolint ignore=DL3007
FROM alpine:latest

# rudolint ignore=DL3007
FROM busybox:latest

# ordinary comments are ignored
RUN echo ok # hadolint ignore=DL4006

# hadolint shell=bash is not a suppression
RUN echo done
