# Metadata Runtime Noise

This fixture represents a minimal runtime image with generated OCI labels and a
non-root entrypoint.

It exists to catch false positives around registry names, generated label
values, and final-user handling.
