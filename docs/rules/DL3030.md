# DL3030 yum-install-assume-yes

- Default severity: warning
- Category: compatibility
- Provenance: Hadolint-compatible Dockerfile rule.
- Autofix: no safe automatic fix is declared because shell command rewriting can change command grouping or option order.

Requires `yum install` invocations to include `-y` or `--assumeyes` so Docker
builds do not stop for interactive prompts.

Bad:

```Dockerfile
RUN yum install curl
```

Good:

```Dockerfile
RUN yum install -y curl
```

Configure with `.rudolint.yaml` `ignore` or `severity` entries when needed.
