# RDL4003 single-cmd

- Default severity: warning
- Category: compatibility
- Provenance: Hadolint-compatible Dockerfile rule.
- Autofix: none yet.

Warns when multiple `CMD` instructions are present because only the final one is
used.

Bad:

```Dockerfile
CMD ["one"]
CMD ["two"]
```

Good:

```Dockerfile
CMD ["two"]
```

Configure with `.rudolint.yaml` `ignore` or `severity` entries when needed.
