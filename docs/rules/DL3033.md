# DL3033 pin-yum-versions

- Default severity: warning
- Category: compatibility
- Provenance: Hadolint-compatible Dockerfile rule.
- Autofix: no safe automatic fix is declared because the correct package version depends on project intent.

Requires direct packages installed with `yum install` to use explicit version
pins.

Bad:

```Dockerfile
RUN yum install -y curl
```

Good:

```Dockerfile
RUN yum install -y curl-8.0.1
```

Configure with `.rudolint.yaml` `ignore` or `severity` entries when needed.
